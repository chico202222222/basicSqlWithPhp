# basicSqlWithPhp

<pre>

<h1 font-family:'JetBrains Mono', monospace> 
1. you must change the database name, user and password in the "gravar.php" file, since it was pre-configured to run 
on my local server.
2. the .php code was implemented looking to avoid falling for SQL Injections (hence why the ? ? ? in the query line)
3. this is for educational purposes; just havin fun ;)


</h1>
</pre>
